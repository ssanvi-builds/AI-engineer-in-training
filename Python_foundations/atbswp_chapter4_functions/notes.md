### Insight 3

It's a good practice to specify the except case to capture a specific type of error. Otherwise, unexpected errors will be captured under the same assumption and no clues for debugging will be given.